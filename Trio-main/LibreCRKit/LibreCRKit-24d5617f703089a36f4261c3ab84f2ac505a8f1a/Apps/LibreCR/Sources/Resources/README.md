# LibreCR App Resources

The public PoC app bundles only static certificate/key artifacts and does not
ship captured sensor state.

`Libre3PatchContext.xml` and `Libre3SensorState.json` are generated or imported
locally during live testing and are intentionally ignored by git.
